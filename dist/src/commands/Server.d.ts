import { type ExecutableCommand, type ValidatorOptions } from "../lib/types";
declare const GenerateServer: ExecutableCommand;
export default GenerateServer;
declare const Validator: ValidatorOptions;
export { Validator };
